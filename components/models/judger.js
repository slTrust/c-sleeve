class judger{

    fenceGroup

    constructor(fenceGroup) {
        this.fenceGroup = fenceGroup;
    }

    initPathDict(){
        this.fenceGroup.spu.sku_list.forEach(s=>{
            s.code
        })
    }

}

export {
    judger
}