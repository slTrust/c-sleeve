<view class="container">
    <view class="title"></view>
    <block wx:for="{{fences}}" wx:key="{{index}}">
        <s-fence bind:celltap="onCellTab" fence="{{item}}"></s-fence>
    </block>
    <view class="counter-container">
        <!-- <l-counter></l-counter> -->
    </view>

    
      
</view>
  